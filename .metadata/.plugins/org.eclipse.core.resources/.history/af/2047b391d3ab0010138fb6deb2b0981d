<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<c:set var="ctx" value="${pageContext.request.contextPath}" />
<!DOCTYPE html>
<html>
<head>
    <title>ABCNews - Trang chủ</title>
    <meta charset="UTF-8"/>
    <link rel="stylesheet" href="${ctx}/assets/css/style.css"/>
    <link rel="icon" type="image/png" href="${ctx}/assets/img/newsicon.png"/>
    <style>
        /* Tùy chỉnh nâng cao cho trang chủ */
        .layout {
            display: grid;
            grid-template-columns: 280px 1fr 340px;
            gap: 28px;
            margin-top: 28px;
            align-items: start;
        }

        .main {
            background: linear-gradient(135deg, #f7fafc, #e6f0fa);
            border-radius: var(--radius);
            padding: 20px;
            box-shadow: var(--shadow);
        }

        .main .main-title {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 2px solid var(--primary);
            font-size: 1.8rem;
            color: var(--text);
            animation: fadeIn 0.5s ease-in;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .news-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
            gap: 24px;
            margin-bottom: 24px;
        }

        .news-card {
            border-radius: var(--radius);
            overflow: hidden;
            background: linear-gradient(180deg, #ffffff, #fbfdff);
            border: 1px solid rgba(16, 24, 40, 0.05);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .news-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 50px rgba(16, 24, 40, 0.15);
        }

        .thumb {
            height: 180px;
            background: #eef5ff;
            overflow: hidden;
            position: relative;
        }
        .thumb img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.4s ease;
        }
        .news-card:hover .thumb img {
            transform: scale(1.1);
        }

        .news-title {
            padding: 14px;
            font-weight: 700;
            color: var(--text);
            font-size: 1.2rem;
            min-height: 72px;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .info {
            padding: 0 14px 14px 14px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-top: 1px solid rgba(16, 24, 40, 0.05);
        }
        .date, .view-count {
            font-size: 0.95rem;
            color: var(--muted);
        }

        /* Sidebar trái */
        .left-sidebar {
            background: linear-gradient(135deg, #ffffff, #f0f7ff);
            padding: 20px;
            position: sticky;
            top: 20px;
        }
        .left-sidebar .search-box {
            display: flex;
            gap: 10px;
            margin-bottom: 16px;
        }
        .left-sidebar input[type="text"] {
            flex: 1;
            padding: 12px;
            border-radius: 10px;
            border: 1px solid #e6eef9;
            background: #fbfdff;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }
        .left-sidebar input[type="text"]:focus {
            border-color: var(--primary);
            outline: none;
        }
        .left-sidebar button {
            padding: 12px 16px;
            border-radius: 10px;
            border: none;
            background: var(--primary);
            color: #fff;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.2s ease;
        }
        .left-sidebar button:hover {
            background: #1e5389;
            transform: translateY(-2px);
        }

        .left-sidebar .cat-btn {
            margin-bottom: 12px;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .left-sidebar .cat-btn:hover {
            transform: translateY(-6px);
            box-shadow: 0 12px 30px rgba(43, 111, 179, 0.12);
        }
        .left-sidebar .cat-btn:last-child {
            margin-bottom: 0;
        }

        /* Sidebar phải */
        .right-sidebar {
            background: linear-gradient(135deg, #ffffff, #f0f7ff);
            padding: 20px;
            position: sticky;
            top: 20px;
        }
        .right-sidebar .list-item {
            margin-bottom: 14px;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .right-sidebar .list-item:hover {
            transform: translateX(5px);
            box-shadow: 0 8px 20px rgba(16, 24, 40, 0.08);
        }
        .right-sidebar .list-item:last-child {
            margin-bottom: 0;
        }
        .right-sidebar .recent-views {
            margin-top: 20px;
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        .right-sidebar .recent-views img {
            width: 70px;
            height: 50px;
            border-radius: 8px;
            transition: transform 0.3s ease;
        }
        .right-sidebar .recent-views img:hover {
            transform: scale(1.1);
        }

        /* Newsletter */
        .newsletter-form {
            margin-top: 24px;
            padding: 16px;
            background: var(--card);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
            display: flex;
            gap: 12px;
            align-items: center;
        }
        .newsletter-form input {
            flex: 1;
            padding: 12px;
            border-radius: 10px;
            border: 1px solid #e6eef9;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }
        .newsletter-form input:focus {
            border-color: var(--primary);
            outline: none;
        }
        .newsletter-form .btn {
            padding: 12px 18px;
            font-weight: 600;
            transition: background 0.3s ease, transform 0.2s ease;
        }
        .newsletter-form .btn:hover {
            background: #1e5389;
            transform: translateY(-2px);
        }

        /* Responsive */
        @media (max-width: 980px) {
            .layout {
                grid-template-columns: 1fr;
            }
            .sidebar.sticky {
                position: relative;
                top: auto;
                margin-bottom: 24px;
            }
            .news-grid {
                grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
            }
            .left-sidebar, .right-sidebar {
                padding: 16px;
            }
        }

        @media (max-width: 640px) {
            .news-grid {
                grid-template-columns: 1fr;
            }
            .newsletter-form {
                flex-direction: column;
                gap: 10px;
            }
            .newsletter-form .btn {
                width: 100%;
            }
        }
    </style>
</head>
<body>
<jsp:include page="/layout/header.jsp"/>

<div class="page-wrap">
    <div class="mobile-toggle" style="display:none; margin-bottom:12px;">
        <button class="btn" onclick="ABC.toggleLeft()">☰ Danh mục</button>
        <button class="btn" onclick="ABC.toggleRight()">☰ Khác</button>
    </div>

    <div class="layout">
        <!-- Left Sidebar -->
        <div id="left-sidebar" class="sidebar left-sidebar sticky">
            <jsp:include page="/layout/leftSidebar.jsp"/>
            <!-- Thêm ô tìm kiếm -->
            <div class="search-box">
                <input type="text" placeholder="Tìm danh mục..." />
                <button>Tìm</button>
            </div>
        </div>

        <!-- Main Content -->
        <main class="main">
            <div class="container">
                <h2 class="main-title"><img src="${ctx}/assets/img/newsicon.png" width="32" alt="Logo"/> Tin nổi bật hôm nay</h2>

                <!-- Menu loại tin -->
                <div class="category-menu" style="margin-bottom:20px;">
                    <c:forEach var="cat" items="${categoryList}">
                        <a href="${ctx}/NewsListServlet?cat=${cat.id}" class="cat-btn">${cat.name}</a>
                    </c:forEach>
                </div>

                <!-- Hot nhất -->
                <h3 class="main-title" style="font-size:1.3rem;margin-bottom:16px;"><span class="icon-fire"></span> Bản tin hot nhất</h3>
                <div class="news-grid">
                    <c:forEach var="news" items="${hotList}">
                        <div class="news-card">
                            <a href="${ctx}/NewsDetailServlet?id=${news.id}" style="text-decoration:none;color:inherit;">
                                <div class="thumb"><img src="${news.image}" alt="${news.title}"/></div>
                                <div class="meta">
                                    <div class="news-title">${news.title}</div>
                                    <div class="info">
                                        <span class="date"><c:out value="${news.postedDate}"/></span>
                                        <span class="view-count">${news.viewCount} lượt xem</span>
                                    </div>
                                </div>
                            </a>
                        </div>
                    </c:forEach>
                </div>

                <!-- Mới nhất -->
                <h3 class="main-title" style="font-size:1.3rem;margin-top:24px;"><span class="icon-new"></span> Bản tin mới nhất</h3>
                <div class="news-grid">
                    <c:forEach var="news" items="${newestList}">
                        <div class="news-card">
                            <a href="${ctx}/NewsDetailServlet?id=${news.id}" style="text-decoration:none;color:inherit;">
                                <div class="thumb"><img src="${news.image}" alt="${news.title}"/></div>
                                <div class="meta">
                                    <div class="news-title">${news.title}</div>
                                    <div class="info">
                                        <span class="date"><c:out value="${news.postedDate}"/></span>
                                        <span class="view-count">${news.viewCount} lượt xem</span>
                                    </div>
                                </div>
                            </a>
                        </div>
                    </c:forEach>
                </div>

                <!-- Newsletter form -->
                <section>
                    <form action="${ctx}/NewsletterServlet" method="post" class="newsletter-form">
                        <input type="email" name="email" placeholder="Nhập email để nhận bản tin mới" required />
                        <button type="submit" class="btn">Đăng ký</button>
                    </form>
                    <c:if test="${not empty success}">
                        <div class="alert success">${success}</div>
                    </c:if>
                    <c:if test="${not empty error}">
                        <div class="alert error">${error}</div>
                    </c:if>
                </section>
            </div>
        </main>

        <!-- Right Sidebar -->
        <aside id="right-sidebar" class="sidebar right-sidebar sticky">
            <jsp:include page="/layout/rightSidebar.jsp"/>
            <!-- Thêm phần tin xem gần đây -->
            <div class="recent-views">
                <c:forEach var="recent" items="${recentViews}" begin="0" end="4">
                    <img src="${recent.image}" alt="Recent view" />
                </c:forEach>
            </div>
        </aside>
    </div>
</div>

<jsp:include page="/layout/footer.jsp"/>
<script src="${ctx}/assets/js/sidebar.js"></script>
</body>
</html>