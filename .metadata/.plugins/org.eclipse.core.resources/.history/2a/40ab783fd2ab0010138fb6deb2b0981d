<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<html>
<head>
    <title>Trang Chủ - ABC News</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .card-img {
            height: 200px;
            object-fit: cover;
        }
    </style>
</head>
<body class="bg-gray-100 font-sans">
    <!-- Header -->
    <header class="bg-blue-600 text-white p-6">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-3xl font-bold">ABC News</h1>
            <nav>
                <a href="#" class="text-white hover:text-gray-200 mx-4">Trang chủ</a>
                <c:if test="${sessionScope.user != null}">
                    <c:choose>
                        <c:when test="${sessionScope.user.role}">
                            <a href="AdminNewsServlet" class="text-white hover:text-gray-200 mx-4">Quản lý</a>
                        </c:when>
                        <c:otherwise>
                            <a href="ReporterNewsServlet" class="text-white hover:text-gray-200 mx-4">Thêm Tin</a>
                        </c:otherwise>
                    </c:choose>
                    <a href="LogoutServlet" class="text-white hover:text-gray-200 mx-4">Đăng xuất</a>
                </c:if>
                <c:if test="${sessionScope.user == null}">
                    <a href="LoginServlet" class="text-white hover:text-gray-200 mx-4">Đăng nhập</a>
                </c:if>
            </nav>
        </div>
    </header>

    <!-- Main Content with Sidebar -->
    <div class="container mx-auto mt-6 flex gap-6">
        <!-- Left Sidebar -->
        <aside class="w-1/5 bg-white shadow-md rounded-lg p-4 sticky top-6 h-[calc(100vh-100px)]">
            <h2 class="text-xl font-semibold mb-4 text-gray-800 border-b pb-2">Danh Mục</h2>
            <ul class="space-y-3">
                <c:forEach items="${catList}" var="cat">
                    <li>
                        <a href="NewsListServlet?catId=${cat.id}" class="text-blue-600 hover:text-blue-800 hover:bg-gray-100 p-2 rounded block transition duration-200">${cat.name}</a>
                    </li>
                </c:forEach>
            </ul>
        </aside>

        <!-- Main Content -->
        <main class="w-3/5 p-4">
            <h2 class="text-2xl font-bold mb-6 text-gray-800 border-b pb-2">Tin Tức Mới Nhất</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <c:forEach items="${newsList}" var="news">
                    <div class="bg-white shadow-lg rounded-lg overflow-hidden hover:shadow-xl transition duration-300">
                        <img src="${news.image}" alt="${news.title}" class="card-img w-full">
                        <div class="p-4">
                            <h3 class="text-lg font-semibold text-gray-800 line-clamp-2">${news.title}</h3>
                            <p class="text-gray-600 mt-2 line-clamp-3">${news.content}</p>
                            <div class="flex justify-between items-center mt-4">
                                <span class="text-sm text-gray-500">Lượt xem: ${news.viewCount}</span>
                                <a href="NewsDetailServlet?id=${news.id}" class="text-blue-600 hover:text-blue-800 font-medium">Xem chi tiết</a>
                            </div>
                        </div>
                    </div>
                </c:forEach>
            </div>
            <c:if test="${empty newsList}">
                <p class="text-center text-gray-500 mt-6">Không có tin tức nào để hiển thị.</p>
            </c:if>
        </main>

        <!-- Right Sidebar -->
        <aside class="w-1/5 bg-white shadow-md rounded-lg p-4 sticky top-6 h-[calc(100vh-100px)]">
            <h2 class="text-xl font-semibold mb-4 text-gray-800 border-b pb-2">Tin Nổi Bật</h2>
            <ul class="space-y-4">
                <c:forEach items="${hotNews}" var="hot">
                    <li class="border-b pb-2">
                        <a href="NewsDetailServlet?id=${hot.id}" class="text-blue-600 hover:text-blue-800 font-medium line-clamp-2">${hot.title}</a>
                        <p class="text-gray-500 text-sm mt-1">Lượt xem: ${hot.viewCount}</p>
                    </li>
                </c:forEach>
            </ul>
            <h2 class="text-xl font-semibold mt-6 mb-4 text-gray-800 border-b pb-2">Tin Mới Nhất</h2>
            <ul class="space-y-4">
                <c:forEach items="${newestNews}" var="newest">
                    <li class="border-b pb-2">
                        <a href="NewsDetailServlet?id=${newest.id}" class="text-blue-600 hover:text-blue-800 font-medium line-clamp-2">${newest.title}</a>
                        <p class="text-gray-500 text-sm mt-1">${newest.postedDate}</p>
                    </li>
                </c:forEach>
            </ul>
        </aside>
    </div>

    <!-- Footer -->
    <footer class="bg-blue-600 text-white p-4 mt-6 text-center">
        <p>&copy; 2025 ABC News. All rights reserved.</p>
    </footer>
</body>
</html>