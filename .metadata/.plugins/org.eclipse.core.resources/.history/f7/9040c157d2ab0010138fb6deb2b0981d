<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<c:set var="ctx" value="${pageContext.request.contextPath}" />
<!DOCTYPE html>
<html>
<head>
    <title>ABCNews - Trang chủ</title>
    <meta charset="UTF-8"/>
    <link rel="stylesheet" href="${ctx}/assets/css/style.css"/>
    <link rel="icon" type="image/png" href="${ctx}/assets/img/newsicon.png"/>
    <style>
        /* Tùy chỉnh bổ sung cho trang chủ */
        .layout {
            display: grid;
            grid-template-columns: 260px 1fr 320px;
            gap: 24px; /* Tăng khoảng cách giữa các cột */
            margin-top: 24px;
            align-items: start;
        }

        .main .main-title {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 16px;
            padding-bottom: 8px;
            border-bottom: 1px solid rgba(16, 24, 40, 0.1);
        }

        .news-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
            gap: 20px; /* Khoảng cách giữa các thẻ tin tức */
        }

        .news-card {
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }
        .news-card:hover {
            transform: translateY(-6px);
            box-shadow: 0 16px 40px rgba(16, 24, 40, 0.12);
        }

        .thumb {
            height: 160px; /* Tăng chiều cao ảnh để nổi bật hơn */
            background: #eef5ff;
        }
        .thumb img {
            transition: transform 0.3s ease;
        }
        .news-card:hover .thumb img {
            transform: scale(1.05);
        }

        .news-title {
            min-height: 60px; /* Tăng chiều cao tiêu đề để chứa nhiều dòng hơn */
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            font-size: 1.1rem;
        }

        .info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 8px;
        }
        .date, .view-count {
            font-size: 0.9rem;
            color: var(--muted);
        }

        /* Sidebar trái */
        .left-sidebar ul {
            list-style: none;
            padding: 0;
        }
        .left-sidebar .cat-btn {
            margin-bottom: 10px;
        }
        .left-sidebar .cat-btn:last-child {
            margin-bottom: 0;
        }

        /* Sidebar phải */
        .right-sidebar .list-item {
            margin-bottom: 12px;
        }
        .right-sidebar .list-item:last-child {
            margin-bottom: 0;
        }
        .right-sidebar .recent-views {
            margin-top: 16px;
        }
        .right-sidebar .recent-views img {
            margin-right: 8px;
        }
        .right-sidebar .recent-views img:last-child {
            margin-right: 0;
        }

        /* Newsletter */
        .newsletter-form {
            margin-top: 20px;
            padding: 12px;
            background: var(--card);
            border-radius: var(--radius);
            box-shadow: var(--shadow);
        }
        .newsletter-form input {
            padding: 12px;
            font-size: 0.95rem;
        }
        .newsletter-form .btn {
            padding: 12px 16px;
            font-weight: 600;
        }

        /* Responsive */
        @media (max-width: 980px) {
            .layout {
                grid-template-columns: 1fr;
            }
            .sidebar.sticky {
                position: relative;
                top: auto;
                margin-bottom: 24px;
            }
            .news-grid {
                grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            }
        }
    </style>
</head>
<body>
<jsp:include page="/layout/header.jsp"/>

<div class="page-wrap">
    <div class="mobile-toggle" style="display:none; margin-bottom:12px;">
        <button class="btn" onclick="ABC.toggleLeft()">☰ Danh mục</button>
        <button class="btn" onclick="ABC.toggleRight()">☰ Khác</button>
    </div>

    <div class="layout">
        <!-- Left Sidebar -->
        <div id="left-sidebar" class="sidebar left-sidebar sticky">
            <jsp:include page="/layout/leftSidebar.jsp"/>
            <!-- Thêm ô tìm kiếm -->
            <div class="search-box">
                <input type="text" placeholder="Tìm danh mục..." />
                <button>Tìm</button>
            </div>
        </div>

        <!-- Main Content -->
        <main class="main">
            <div class="container">
                <h2 class="main-title"><img src="${ctx}/assets/img/newsicon.png" width="28" alt="Logo"/> Tin nổi bật hôm nay</h2>

                <!-- Menu loại tin -->
                <div class="category-menu" style="margin-bottom:18px;">
                    <c:forEach var="cat" items="${categoryList}">
                        <a href="${ctx}/NewsListServlet?cat=${cat.id}" class="cat-btn">${cat.name}</a>
                    </c:forEach>
                </div>

                <!-- Hot nhất -->
                <h3 class="main-title" style="font-size:1.15rem;margin-bottom:10px;"><span class="icon-fire"></span> Bản tin hot nhất</h3>
                <div class="news-grid">
                    <c:forEach var="news" items="${hotList}">
                        <div class="news-card">
                            <a href="${ctx}/NewsDetailServlet?id=${news.id}" style="text-decoration:none;color:inherit;">
                                <div class="thumb"><img src="${news.image}" alt="${news.title}"/></div>
                                <div class="meta">
                                    <div class="news-title">${news.title}</div>
                                    <div class="info">
                                        <span class="date"><c:out value="${news.postedDate}"/></span>
                                        <span class="view-count">${news.viewCount} lượt xem</span>
                                    </div>
                                </div>
                            </a>
                        </div>
                    </c:forEach>
                </div>

                <!-- Mới nhất -->
                <h3 class="main-title" style="font-size:1.15rem;margin-top:18px;"><span class="icon-new"></span> Bản tin mới nhất</h3>
                <div class="news-grid">
                    <c:forEach var="news" items="${newestList}">
                        <div class="news-card">
                            <a href="${ctx}/NewsDetailServlet?id=${news.id}" style="text-decoration:none;color:inherit;">
                                <div class="thumb"><img src="${news.image}" alt="${news.title}"/></div>
                                <div class="meta">
                                    <div class="news-title">${news.title}</div>
                                    <div class="info">
                                        <span class="date"><c:out value="${news.postedDate}"/></span>
                                        <span class="view-count">${news.viewCount} lượt xem</span>
                                    </div>
                                </div>
                            </a>
                        </div>
                    </c:forEach>
                </div>

                <!-- Newsletter form -->
                <section style="margin-top:20px;">
                    <form action="${ctx}/NewsletterServlet" method="post" class="newsletter-form">
                        <input type="email" name="email" placeholder="Nhập email để nhận bản tin mới" required />
                        <button type="submit" class="btn">Đăng ký</button>
                    </form>
                    <c:if test="${not empty success}">
                        <div class="alert success">${success}</div>
                    </c:if>
                    <c:if test="${not empty error}">
                        <div class="alert error">${error}</div>
                    </c:if>
                </section>
            </div>
        </main>

        <!-- Right Sidebar -->
        <aside id="right-sidebar" class="sidebar right-sidebar sticky">
            <jsp:include page="/layout/rightSidebar.jsp"/>
            <!-- Thêm phần tin xem gần đây -->
            <div class="recent-views">
                <c:forEach var="recent" items="${recentViews}" begin="0" end="3">
                    <img src="${recent.image}" alt="Recent view" />
                </c:forEach>
            </div>
        </aside>
    </div>
</div>

<jsp:include page="/layout/footer.jsp"/>
<script src="${ctx}/assets/js/sidebar.js"></script>
</body>
</html>